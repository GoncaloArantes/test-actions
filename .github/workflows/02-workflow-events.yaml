name: 02 - Workflow Events

on: 
  schedule:
    - cron: "*/2 * * * *" # Every 2 minutes

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo "This workflow's trigger event is: ${{ github.event_name }}"
